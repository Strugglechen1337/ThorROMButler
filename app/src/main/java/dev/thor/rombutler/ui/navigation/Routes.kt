package dev.thor.rombutler.ui.navigation

/**
 * Navigation routes of the app. Plain string routes are sufficient for the
 * small, linear flow of v0.1 (Setup -> Scan -> Review -> Log).
 */
object Routes {
    const val SETUP = "setup"
    const val SCAN = "scan"
    const val REVIEW = "review"
    const val LOG = "log"
    const val SETTINGS = "settings"
    const val DOCTOR = "doctor"
}
